# next-app

Public MTL Archives site (Next.js on Vercel): search, game, prints, newsletter, `/research`.

```bash
npm run dev --workspace=apps/next-app
```

API origin is `NEXT_PUBLIC_API_URL` (Cloudflare Worker).
