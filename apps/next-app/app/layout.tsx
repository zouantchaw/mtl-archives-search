import type { Metadata, Viewport } from "next";
import { Figtree } from "next/font/google";
import Script from "next/script";
import "./globals.css";
import { CartProvider } from "@/lib/cart-context";
import { CartDrawer } from "@/components/CartDrawer";

const figtree = Figtree({
  subsets: ['latin'],
  variable: '--font-sans',
  display: 'swap',
});

const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || 'https://mtlarchives.com';

export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  maximumScale: 5,
  userScalable: true,
  viewportFit: 'cover',
  themeColor: [
    { media: '(prefers-color-scheme: light)', color: '#fafafa' },
    { media: '(prefers-color-scheme: dark)', color: '#0a0a0a' },
  ],
};

export const metadata: Metadata = {
  metadataBase: new URL(siteUrl),
  title: {
    default: 'MTL Archives — Photos historiques de Montréal',
    template: '%s | MTL Archives',
  },
  description: 'Explorez 14 822 photos historiques de Montréal. Recherchez par rue, quartier ou lieu emblématique. Explore 14,822 historical photos of Montreal.',
  keywords: [
    'Montreal', 'Montréal', 'archives', 'photos', 'historical', 'historique',
    'history', 'histoire', 'Quebec', 'Québec', 'vintage', 'old photos',
    'Vieux-Montréal', 'Old Montreal', 'patrimoine', 'heritage'
  ],
  authors: [{ name: 'MTL Archives' }],
  creator: 'MTL Archives',
  publisher: 'MTL Archives',
  
  // Open Graph - images are auto-generated by opengraph-image.tsx
  openGraph: {
    type: 'website',
    locale: 'fr_CA',
    alternateLocale: 'en_CA',
    url: siteUrl,
    siteName: 'MTL Archives',
    title: 'MTL Archives — Photos historiques de Montréal',
    description: 'Explorez 14 822 photos historiques de Montréal. Recherchez par rue, quartier ou lieu emblématique.',
    // Images auto-generated by opengraph-image.tsx (1200x630)
  },
  
  // Twitter Card - images are auto-generated by twitter-image.tsx
  twitter: {
    card: 'summary_large_image',
    site: '@mtlarchives',
    creator: '@mtlarchives',
    title: 'MTL Archives — Photos historiques de Montréal',
    description: 'Explorez 14 822 photos historiques de Montréal.',
    // Images auto-generated by twitter-image.tsx (1200x630)
  },
  
  // Additional meta for broader platform support
  other: {
    'apple-mobile-web-app-title': 'MTL Archives',
    'application-name': 'MTL Archives',
    'msapplication-TileColor': '#171717',
    'theme-color': '#fafafa',
  },
  
  // Icons - using Next.js generated icons
  // icon.tsx, apple-icon.tsx handle dynamic generation
  
  // App configuration
  appleWebApp: {
    capable: true,
    statusBarStyle: 'default',
    title: 'MTL Archives',
  },
  
  // Other
  formatDetection: {
    telephone: false,
  },
  
  // Robots
  robots: {
    index: true,
    follow: true,
    googleBot: {
      index: true,
      follow: true,
      'max-video-preview': -1,
      'max-image-preview': 'large',
      'max-snippet': -1,
    },
  },
  
  // Verification (add your IDs when ready)
  // verification: {
  //   google: 'your-google-verification-code',
  // },

  // Alternate languages for hreflang
  alternates: {
    canonical: siteUrl,
    languages: {
      'fr-CA': `${siteUrl}?lang=fr`,
      'en-CA': `${siteUrl}?lang=en`,
      'x-default': siteUrl,
    },
  },
};

// Organization JSON-LD for homepage
const organizationJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'MTL Archives',
  url: siteUrl,
  logo: `${siteUrl}/icon-512.png`,
  description: 'Explorez 14 822 photos historiques de Montréal. Recherchez par rue, quartier ou lieu emblématique.',
  sameAs: [
    'https://twitter.com/mtlarchives',
  ],
};

// CollectionPage JSON-LD for the photo archive
const collectionJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  name: 'MTL Archives — Photos historiques de Montréal',
  description: 'Collection de 14 822 photos historiques des archives de la Ville de Montréal.',
  url: siteUrl,
  publisher: {
    '@type': 'Organization',
    name: 'Archives de la Ville de Montréal',
  },
  about: {
    '@type': 'Place',
    name: 'Montréal, Québec, Canada',
  },
  inLanguage: ['fr-CA', 'en-CA'],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${figtree.variable}`}>
      <head>
        <Script
          src="https://cdn.seline.com/seline.js"
          data-token="ba441af9ffc2d81"
          strategy="afterInteractive"
        />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{
            __html: JSON.stringify(organizationJsonLd),
          }}
        />
        <script
          type="application/ld+json"
          dangerouslySetInnerHTML={{
            __html: JSON.stringify(collectionJsonLd),
          }}
        />
      </head>
      <body className="font-sans antialiased bg-neutral-50 text-neutral-900 min-h-screen">
        <CartProvider>
        {children}
          <CartDrawer />
        </CartProvider>
      </body>
    </html>
  );
}
