{
  "version": "vision-gateway-v1",
  "issue": 148,
  "hypothesis": "Frontier OpenAI and xAI vision models, reached only through Cloudflare AI Gateway Unified Billing, improve factual features and viewpoint on the unchanged 50-image diagnostic contract without training or production promotion.",
  "sample": "Same 50 image bytes and prompt as the #139 Mistral/Gemma comparison. Diagnostic, not independent holdout.",
  "models": ["openai/gpt-5.4", "xai/grok-4.6"],
  "verified_at": "2026-09-13",
  "endpoint": "https://api.cloudflare.com/client/v4/accounts/{account}/ai/run",
  "auth": "Cloudflare API token; Unified Billing credits; no provider keys on the request",
  "cache": "cf-aig-skip-cache=true on every call; cache hits must be identified if they still occur",
  "max_tokens": 4096,
  "openai_token_field": "max_completion_tokens",
  "budget_usd": 8.5,
  "staging": "Two-image smoke per model before the 50-image run. Stop on HTTP 402/429.",
  "promotion": false
}
