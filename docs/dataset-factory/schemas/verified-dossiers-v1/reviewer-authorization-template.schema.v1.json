{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "status",
    "artifact_id",
    "candidate_descriptor_sha256",
    "packet_manifest_sha256",
    "review_scope",
    "approved_reviewer",
    "authorizing_authority",
    "authorized_at",
    "forbidden_principals"
  ],
  "properties": {
    "schema_version": {
      "const": "verified_dossiers_reviewer_authorization_template_v1.0.0"
    },
    "status": { "const": "blank_coordinator_authorization_required" },
    "artifact_id": { "type": "string" },
    "candidate_descriptor_sha256": { "type": "null" },
    "packet_manifest_sha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "review_scope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "scope_id",
        "dossier_ids",
        "allowed_dispositions",
        "categorically_ineligible_numeric_ids",
        "minimum_eligible_acceptances"
      ],
      "properties": {
        "scope_id": { "type": "string" },
        "dossier_ids": {
          "type": "array",
          "minItems": 36,
          "maxItems": 36,
          "uniqueItems": true,
          "items": { "type": "string" }
        },
        "allowed_dispositions": { "const": ["accepted", "held", "rejected"] },
        "categorically_ineligible_numeric_ids": {
          "const": [8132, 8134, 8139, 8143]
        },
        "minimum_eligible_acceptances": { "const": 25 }
      }
    },
    "approved_reviewer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["reviewer_id", "session_id", "model", "reasoning_effort"],
      "properties": {
        "reviewer_id": { "const": "" },
        "session_id": { "const": "" },
        "model": { "const": "gpt-5.6-sol" },
        "reasoning_effort": { "const": "high" }
      }
    },
    "authorizing_authority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identity", "session_id", "role"],
      "properties": {
        "identity": { "const": "" },
        "session_id": { "const": "" },
        "role": { "const": "gate_g_review_authority" }
      }
    },
    "authorized_at": { "const": "" },
    "forbidden_principals": {
      "type": "object",
      "additionalProperties": false,
      "required": ["identities", "sessions", "excluded_roles"],
      "properties": {
        "identities": {
          "type": "array",
          "minItems": 3,
          "uniqueItems": true,
          "items": { "type": "string" }
        },
        "sessions": {
          "type": "array",
          "minItems": 3,
          "uniqueItems": true,
          "items": { "type": "string" }
        },
        "excluded_roles": {
          "type": "array",
          "minItems": 4,
          "uniqueItems": true,
          "items": { "type": "string" }
        }
      }
    }
  }
}
