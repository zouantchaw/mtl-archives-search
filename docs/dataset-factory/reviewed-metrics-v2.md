# Reviewed Metrics v2 Candidate

Reviewed Metrics v2 is a candidate-only blind evaluation contract for issue #96. It freezes 36 image-mode tasks, two OCR crops, six neutral entity scenes, 16 aerial land-use tasks, and 18 abstention decisions before prediction. These 78 task memberships resolve to 44 unique source pixels. A separate source-only contract uses an opaque official-source-search task ID. The candidate and both issue completion booleans are false. No real authority, prediction, freeze, private expected envelope, gold, score, task review, publication, or production mutation exists.

The coordinator input authority contains the exact source hashes, normalized pixel hashes, dimensions, component/split bindings, nullable per-input rights values, predecessors, fixed membership, and source-to-opaque mapping. It must never be copied into a predictor route. The normal bundle command reads only the committed `input-authority-v2.json` at its fixed path and requires exactly 56,908 bytes with SHA-256 `34be01a2750894eab27ad8882acb79a0366f1de9b6c85fb96bb84e95ecfd81fa`; there is no `--authority` override. It also regenerates and compares the complete semantic authority, including the exact `v2-0001` through `v2-0044` mapping. This is non-circular because the authority records `candidate_commit=null`; a later execution authorization separately binds the reviewed candidate commit.

Rights values are copied exactly: `null` is not coerced to `false`, and completeness is not invented. The three Phase D controls are read from the registered ignored Gold evidence route and verified against the planned exact bytes; no source payload is duplicated in Git and no private object locator is recorded in the candidate. All six ground-scene mappings join by neutral ID through exact `ground-originals-v1/records-v1.json` bytes. Both OCR crops additionally join their parent, record, region coordinates, and crop hash through exact `reviewed-visual-transcriptions-v1.json` bytes. Both authorities are pinned globally and per applicable input.

The candidate records implementation base `5fe4dfbe51a320a51f1f126b4a2d8cf0722be5dc` separately from `candidate_commit=null`. This avoids a self-referential fixture hash. A later reviewed authority must bind the actual candidate commit before any execution path can exist.

## Blind bundle

The caller supplies a new absolute temporary route whose parent already exists. Every existing ancestor is checked with `lstat` and `realpath`; symlink parents/leaves, traversal, existing outputs, repository/private/PKM overlap, extra files, duplicate opaque IDs/hashes, source drift, normalized-pixel drift, metadata, and authority substitution are refused. The output directory is created exclusively with an invocation-secret owner marker and inode/device binding. A failure recursively removes only that newly created child after marker and inode verification; an arbitrary caller-supplied route is never recursively removed.

Each source is decoded and written as deterministic lossless PNG with an opaque filename. Every ancillary PNG chunk, including `pHYs`, is stripped; verification requires only critical chunks and zero EXIF, IPTC, XMP, comments, ICC profiles, or thumbnails. The predictor-visible route contains exactly 44 uniformly shaped neutral media rows, neutral instructions requiring the same full-superset output for every row, the exact tracked `prediction-output.schema.v2.json` bytes, and a descriptor that binds those schema bytes. It exposes no purpose, subset, membership, source mapping, per-task count, or self-authored mount attestation. Semantic no-inference tests reject task-family words in descriptor/instructions.

```bash
npm run dataset-factory:reviewed-metrics-verify-v2
npm run dataset-factory:reviewed-metrics-build-blind-bundle-v2 -- --output /tmp/new-opaque-route
npm run dataset-factory:reviewed-metrics-build-source-search-bundle-v2 -- --output /tmp/new-public-source-route
npm run dataset-factory:reviewed-metrics-measure-route-v2 -- --authority /absolute/execution-authorization-v2.json --role private_evaluator --stage private_prepare --output /absolute/private-prepare-measurement.json
npm run dataset-factory:reviewed-metrics-sign-route-receipt-v2 -- --authority /absolute/execution-authorization-v2.json --measurement /absolute/private-prepare-measurement.json --role private_evaluator --stage private_prepare --signing-key /absolute/coordinator-ed25519.pem --expires-at <UTC-within-5-minutes> --output /absolute/private-prepare-route-receipt.json
npm run dataset-factory:reviewed-metrics-stage-run-v2 -- --authority /absolute/execution-authorization-v2.json --stage visual_predict --stage-receipt /absolute/visual-predict-route-receipt.json
npm run dataset-factory:reviewed-metrics-prepare-private-score-v2 -- --workspace /absolute/evaluator-workspace --envelope /absolute/private-expected-envelope-v2.json --detail /absolute/private-score-detail-v2.json --handoff /absolute/private-score-preparation-handoff-v2.json --output /absolute/private-score-receipt-v2.json --signing-key /absolute/evaluator-ed25519.pem --stage-receipt /absolute/private-prepare-route-receipt.json
npm run dataset-factory:reviewed-metrics-retain-private-score-v2 -- --workspace /absolute/evaluator-workspace --envelope /absolute/private-expected-envelope-v2.json --detail /absolute/private-score-detail-v2.json --handoff /absolute/private-score-preparation-handoff-v2.json --output /absolute/private-retention-receipt-v2.json --signing-key /absolute/evaluator-ed25519.pem --stage-receipt /absolute/r2-retain-route-receipt.json
npm run dataset-factory:reviewed-metrics-finalize-private-score-v2 -- --workspace /absolute/evaluator-workspace --envelope /absolute/private-expected-envelope-v2.json --detail /absolute/private-score-detail-v2.json --handoff /absolute/private-score-preparation-handoff-v2.json --retention-receipt /absolute/private-retention-receipt-v2.json --output /absolute/private-score-receipt-v2.json --signing-key /absolute/evaluator-ed25519.pem --stage-receipt /absolute/private-finalize-route-receipt.json
npm run dataset-factory:reviewed-metrics-seal-stage-ledger-v2 -- --authority /absolute/execution-authorization-v2.json --signing-key /absolute/coordinator-ed25519.pem --output /absolute/stage-execution-ledger-v2.json
npm run dataset-factory:reviewed-metrics-security-self-test-v2
npm run dataset-factory:reviewed-metrics-self-test-v2
npm run dataset-factory:reviewed-metrics-integration-test-v2
npm run dataset-factory:reviewed-metrics-verify-tracked-v2
npm run dataset-factory:artifacts:check -- --verify-files --verify-required-only --require dfv0_reviewed_metrics_v1_publication,dfv0_reviewed_metrics_v2_candidate_20260715
npm run typecheck --workspace=@mtl-archives/scripts
```

Prediction, gold, result, source-search, criterion-matrix, publication, and task-review contracts fail closed on completed states. Visual gold retains the exact 36/2/6/16/18 universes, but it contains no source-task decision or private expected commitment. Before prediction, the source-only route receives a public organization-name query subject, `The Gazette`, plus a neutral prompt, an opaque task ID, a generic HTTPS Quebec-government hostname-suffix policy, a generic deterministic normalization contract, and exact bundle-local output-schema bytes. The subject is a public task input selected from the accepted official-source chain so a bundle-only researcher can identify the record; it is neither a scored answer field nor gold. The bundle contains no year, civic number, street, city/place, canonical target URL, repository path, Gate E identifier, dossier, commitment, salt, predecessor pin, source-specific hostname/path, or post-review artifact. The two-file bundle bytes, inventory, schema, and deterministic joins are code/tool evidence. Repository and history denial is instead an operational requirement attested and evidenced on the isolated Kami/Crabbox surface by a reviewed sanitized sync manifest, fresh route and remote inventory, then cleanup/readback; the validator does not prove the remote filesystem state. Its output contains only public year/address/place/official-URL fields and a bounded paraphrase; it does not invent or echo internal source or representation IDs.

Both the visual and source-search predictions are frozen before any completed visual gold review or source dossier/private-envelope work. The dossier follows source freeze; envelope sealing follows the dossier; private scoring is strictly after the envelope seal. Authority identities and timestamps join that order through results, matrix, and publication validation. The private envelope has an exact 32-byte lowercase-hex salt and CSPRNG metadata explicitly recorded under an `attestation_id` as an author attestation rather than an independently verifiable receipt or proof. Salt generation must satisfy `source_search_freeze_at < generated_at <= authored_at == private_envelope_sealed_at`; weak, repeated, low-diversity, sequential, known deterministic, and non-CSPRNG declarations are rejected. The internal synthetic capability alone may use one derived high-diversity deterministic test salt. The commitment is `SHA256("gate-h-v2-search-expected\0" || salt || "\0" || JCS(expected_answer_v2))`.

The deterministic private scorer opens that commitment, validates the exact frozen prediction, and compares its public fields against the post-freeze private envelope under the bundle-visible normalization contract: NFKC and whitespace normalization, case/diacritic/punctuation-insensitive text comparison, common French/English street-designator normalization, digit normalization, and canonical official HTTPS URL comparison. Provenance is not accepted from tracked constants: it dynamically joins the independently authored dossier/envelope to the sole accepted Gate E promotion and independent review receipt, representation, and acquisition. Gate E history remains tracked and readable. The salt and commitment protect envelope integrity and preserve blinding across the ordered workflow; they do not claim secrecy from a reader who independently inspects that history.

Protected finalization opens each authority/task/bundle/prediction/freeze/envelope path descriptor-first with `O_NOFOLLOW`; `fstat` must show an owner-controlled regular one-link file at exact mode `0400` or `0600`, and the path must still name that descriptor. Parsing, hashing, scoring, and final checks use the same retained bytes, including exact rereads that reject same-inode, same-size, restored-mtime rewrites. Canonical private detail is created `O_CREAT|O_EXCL` at `0400`, then the file and directory are fsynced and exact-read back. The evaluator key follows the same descriptor-first/path-inode discipline, requires one link and exact mode `0600`, must be Ed25519 and derive the authority public key, and must remain inside the evaluator root but outside repository and PKM trees.

`prepare-private-score` takes its actual score time from the process clock, generates one finalization ID, writes the sealed detail and signed preparation handoff once at mode `0400`, and stops without a public receipt. `retain-private-score` requires the S3 endpoint to equal the endpoint derived from the same Cloudflare account inspected by the privacy API and requires a dedicated private-bucket identity. A private capability supplies at least 256 bits of high-entropy HMAC key material; keyed opaque addresses prevent a public content SHA-256 from reconstructing the raw R2 key.

PUT custom metadata binds exact SHA-256, bytes, candidate, and finalization. HEAD must match it. GET sends exact `If-Match` and `VersionId` when present, must return matching ETag/version/metadata and bytes, and a final HEAD proves stable identity. A successful conditional PUT records `attempted_at`, `completed_at`, and `status=new_write`; only that case records `written_at`. A 409/412 recovery must prove identical metadata and bytes and records `status=preexisting_verified` plus `observed_existing_at`, never a fictitious original write time. Retention chronology is anchored to verification completion.

Privacy preflight and postflight use endpoint-specific Cloudflare v4 contracts pinned to the primary API shapes reviewed on 2026-07-15. Workers scripts, per-script settings/subdomains, dispatch namespaces, and zone routes are SinglePage responses; Pages projects, Workers custom domains, and account zones use pagination only where the documented endpoint supplies it. R2 custom domains require `domain`, `enabled`, structured ownership/SSL `status`, and only the documented optional cipher/TLS/zone fields; managed domains require `bucketId`, `domain`, and `enabled`. Exposure derives only from `enabled`. Because Cloudflare exposes no documented complete dispatch-script listing endpoint, any returned dispatch namespace fails closed. Pages production/preview R2 bindings, domains, zones, routes, settings, and complete private identities/routing fields are normalized into the private inventory digest; public evidence contains only counts, status-code/inventory digests, and verdicts. Duplicate identities, incomplete pages, unknown fields/bindings, same-count substitutions, permission errors, pre/postflight drift, target-bucket bindings, or enabled custom/managed domains fail closed.

The actual HMAC-derived R2 object key remains private in process memory and the private retention receipt. Public retention exposes only a domain-separated SHA-256 commitment to that key; it never publishes the key, basename, prefix, or any prefix-sufficient locator. Actual `issued_at` is captured only after PUT recovery or creation, HEAD, conditional GET, stable HEAD, and postflight complete. The complete unsigned semantic receipt is validated before signing and exclusive write. Finalization takes its own actual process time and revalidates retention verification completion before issuance and finalization.

Production retention obtains `CLOUDFLARE_ACCOUNT_ID`, `GATE_H2_R2_BUCKET`, `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_R2_ENDPOINT`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `GATE_H2_R2_CAPABILITY_ID`, `GATE_H2_R2_OBJECT_HMAC_KEY`, and `GATE_H2_R2_DEDICATED_BUCKET_ID` from the approved process environment. These values must not be placed in argv, receipts, logs, tracked files, or shell history. The integration suite runs prepare, retain, and finalize as three separate CLI processes with actual advancing clocks and an internal no-network store. Envelope/detail remain excluded from publication.

OCR gold normalization is not an authored target: `ocr_normalized` must exactly equal the documented deterministic normalization of `ocr_raw`, and the scorer derives its gold sequence from `ocr_raw`. Exact match compares those derived sequences; CER and WER use Levenshtein edits over Unicode code points and whitespace-delimited normalized words. A zero gold sequence has an honest zero denominator and undefined rate rather than an invented denominator, although completed v2 gold rejects empty OCR support. Entity mentions require nonblank normalized surfaces, positive-area `[x1,y1,x2,y2]` boxes within `[0,1]`, controlled types, and at most 12 mentions per row. Duplicate rejection quantizes each coordinate to 1/10,000 and also rejects same normalized surface/type pairs with IoU at least `0.98`, closing epsilon-shift inflation while allowing clearly disjoint repeated visible text. Detection precision/recall uses bounded deterministic polynomial min-cost assignment, maximizing valid match cardinality first, integerized total IoU second, and a stable pair tie-break; compatibility still requires exact normalized surface/type and IoU at least `0.5`. Every linked identity is evaluated only against its matched gold mention, and an unmatched linked assertion is unsafe.

The place metric universe is exactly `[task-7b1d4f0c9a2e6d83]`, the opaque SOURCE-ONLY official-search task. Precision is `correct supported source tasks / predicted source tasks`; coverage is `predicted source tasks / reviewed supported source tasks`. The task is joined through the accepted Gate E claim, source representation/acquisition, frozen source prediction, private envelope/detail retention, and signed finalization. Visual prediction `place_links` and visual gold `place_opportunities` are schema- and runtime-forbidden, so arbitrary scene or mention attachment cannot create support. `no_pixel_identity_claim` remains true: this metric says nothing about scene geolocation. Scene-level place opportunity support is zero/unavailable unless a future independent scene-level authority is introduced.

## Complete metric universe

Completed results pin the committed authority copy, raw visual prediction/freeze, gold, exact source task, and signed public private-score receipt. Validation deterministically recomputes exactly 63 rows: OCR normalized exact match/CER/WER; entity precision/recall/false-identity rate; reviewed-source-only place precision/coverage; per-class precision/recall/F1 for all five image classes plus macro-F1 and raw confusion/support; aerial exact-set accuracy/Jaccard, eight-label precision/recall/F1, and micro precision/recall/F1; abstention coverage/rate, appropriate-abstention recall, unsafe non-abstention rate, selective error, and decision accuracy; mask-IoU and geolocation-distance prerequisite N/A rows; actual visual/source operation timing; and explicitly unavailable model/tool cost. Reviewed Metrics v2 has no usage-receipt ingestion path and accepts only that explicit unavailable cost state.

Every row carries the exact fixed and included opaque IDs, exclusions fixed before scoring, raw counts, numerator, denominator, value or honest undefined/N/A, limitations, support, and provenance. Failed predictions and abstentions remain in their applicable fixed denominators. Aerial rows retain the exact 16-ID universe while explicitly excluding only IDs the independent gold review marked unreviewable before scoring; the included/excluded partition is complete and at least 12 IDs must remain reviewable. Mask and geolocation rows include no IDs and explicitly exclude their full fixed universes because the required reviewed masks or verified coordinates do not exist. Completion is derived from the exact metric universe plus all five positive image-class gold supports, positive entity gold support, at least 12 reviewable aerial inputs, and stop conditions including zero entity prediction support, leakage, route overlap, or chronology failure. Honest zero-support states remain visible; mask/geolocation N/A and cost-unavailable-without-receipt are the only expected non-observed final statuses. The normal `score` CLI becomes available only after both frozen prediction chains, completed gold, a signed passing private receipt, and byte-identical authority workspace copy validate. Reusable results validation requires `privateReceipt.scored_at < results.scoring.scored_at`, and matrix/publication inherit that check.

Final matrix rows use exact criterion IDs, result IDs, evidence roles, and filenames. A final matrix is invalid unless it pins the coordinator-signed 12-stage ledger and that envelope verifies independently from local caches and output paths. The twelfth stage is `publication_assembly_plan`: its canonical plan contains the exact sorted 13-path pre-plan universe and matching physical file pins, final 17-member universe, schema pins, authority, publisher, and D1 ledger pin. Extras, omissions, and reordering fail before completion. After the plan stage completes, `seal-stage-ledger` seals complete attested D1 readback. The coordinator-only `assemble-publication` command consumes that canonical plan and signed ledger, exclusively creates and validates the exact matrix, safely opens every finalized pre-descriptor member without following symlinks, and retains each descriptor and exact byte buffer. It constructs and fully validates the descriptor in memory while its path is absent, then revalidates the exact 16-path universe, every retained path/inode/link count/byte pin, the plan and ledger, and the rederived tree immediately before the sole `O_EXCL` descriptor write. No semantic validation follows that write. A preexisting descriptor, concurrent assembler, same-name byte mutation, inode replacement, symlink, hardlink, plan/ledger swap, or forced late directory race fails closed without creating a new descriptor. The descriptor is not its own predecessor. Published output has exactly 17 public members and excludes private envelope/detail, raw provider IDs, credentials, filesystem paths, route nonce/invocation, host locator, and object key.

The freeze implementation remains unavailable because the tracked execution authority and all real pre-activation artifacts do not exist. Production authority loading accepts only v2.4. A later authority-only commit retains the exact direct-child, sole changed path, clean repository/index/worktree, and byte-identical HEAD/index/worktree controls, and must pin three canonical files that already exist in its parent: a signed live D1 attestation, a signed Linux/Kami sandbox attestation, and a separately signed pre-activation verification receipt. Their exact bytes, schema hashes, principals, sessions, logical routes, physical-route identity digests, and public-key hashes are authority fields. A physical-route identity digest commits canonical realpath, nearest existing canonical ancestor, device, and inode; it does not hash the caller spelling. The reviewed trust registry admits exactly one entry for each of `d1_live`, `linux_sandbox`, and `pre_activation_verifier`; all five identity dimensions must be pairwise distinct. Every entry pins a canonical signed admission receipt that already exists at its named strict-ancestor commit. Validation asks trusted `git` to prove that commit exists, is an ancestor but not current HEAD, and contains the exact receipt bytes. The registry is intentionally empty with status `blocked_no_real_attestors_admitted` in this candidate, making verification and activation structurally unavailable. Validation rejects absent, noncanonical, unsigned, stale, future-dated, synthetic, placeholder, self-authored, untrusted, wrong-kind, wrong logical/physical route, principal, session, key, admission receipt, or commit ancestry. Internal v2.3 authorities remain test-only and cannot pass production activation. Source-search activation also requires one coordinator Ed25519 trust key plus exactly seven role inventory entries, each referenced exactly once with no omission, duplicate reference, or unreferenced extra. Every entry carries coordinator-signed physical-host evidence and a verification pin. Local Mac evidence binds the stable hardware UUID plus measured OS/host evidence digest and remains a coordinator-verified machine claim, not a cloud-provider cryptographic attestation. AWS/Kami evidence has the higher provider-attested assurance level: IMDSv2 returns the exact identity document and PKCS7, OpenSSL verifies the attached bytes with `-nointern` against the exact region-allowed AWS DSA certificate, the DER certificate fingerprint is pinned, and the signed document must exactly bind account, region, instance, image, and pending time within the freshness limit. Wrong certificate, signature, signed bytes, field binding, or freshness fails closed. The tracked registry records the primary AWS sources and currently supports only its explicitly listed regions; any other region is refused until its official certificate is reviewed and added. Caller-supplied UUIDs, labels, SSH names, or keys alone are not identity.

The canonical physical-host ID derives only from the normalized Mac hardware UUID or AWS account/region/instance identity. Exact production stages are `visual_predict`, `visual_freeze`, `source_predict`, `source_freeze`, `gold_review`, `gold_envelope_authoring`, `private_prepare`, `r2_retain`, `private_finalize`, `task_review`, `metrics_score`, and `publication_assembly_plan`, each bound to its owning role. Authority pins only domain-separated D1 account/database capability digests, the canonical deployed `sqlite_master` table/index/trigger attestation hash and bytes, namespace, API contract, and coordinator signer. Raw IDs, tokens, and filesystem ledger locations are forbidden from authority/public evidence. Attempts, claims, and completions use composite candidate+authority+stage+attempt joins; claims remain unique per candidate+authority+stage; all six UPDATE/DELETE triggers are mandatory. The adapter attests the deployed schema before each begin/completion and again before sealing. Stage executors receive no D1 capability; coordinator orchestration performs fixed INSERT mutations and SELECT readback only.

Before stage work, the coordinator validates the authority/route window and deployed D1 schema, appends an attempt, claims the unique stage, and verifies readback. Completion repeats schema/host/time checks, hashes exact outputs, appends completion, and verifies readback. Sealing requires exactly one attempt/claim/completion for each ordered stage; every composite identity, canonical envelope hash, request hash, and one-to-one join must match. Signed public evidence carries the ordered auditable attempt entries and their set commitment. Local files remain non-authoritative.

External operations require an exact clean physical Git cwd with no modified or untracked content, role-bearing pins for the wrapper, runtime, script, dependency lock, package manifest, installed dependency-tree document, and every readable artifact. The installed-tree schema requires a complete resolution contract derived by the named Node runtime, npm lockfile/layout, static-plus-runtime module resolver, and Linux ELF loader discovery rules. Its physical roots must equal the runtime, entrypoints, lockfile, package manifest, resolved package roots, and resolved native-library roots exactly; every package/native root must be justified by an import or loader edge whose importer/consumer and target are signed members. It carries the resolver proof commitments, no unresolved or dynamic edges, the sorted complete member set, and root/closure/tree digests. Preflight recursively enumerates each no-symlink root and requires exact equality; it reopens every member and re-enumerates the roots immediately before spawn, so replacement or a newly inserted import candidate fails before child output or ledger completion.

Wrapper arguments use `gate_h2_linux_wrapper_argv_v3`. The immutable image identity kind, immutable identity, digest, every complete read-only/output mount tuple including content hash, network policy/namespace/firewall tuple, full runtime pin, and every destination capability are deterministically encoded with signed semantic fields and join hashes. A fixed argument is allowed only as an exact value-pinned `enum`, `integer`, `sha256`, `identifier`, or `boolean`; unknown types and arguments fail. Main argv/environment literals use the same syntax-specific scalar allowlist. Consequently filesystem-resolvable basenames and dotfiles such as `.env`, `package.json`, `config.json`, and `secret.pem`, as well as absolute/relative paths, separators, traversal, URI/file forms, and combined `--name=PATH` values, cannot be literals. Readable values must join a named artifact/read-only mount and outputs a declared writable mount. Public `HOME`, Codex/Claude config, auth, token, secret, credential, and access-key values are forbidden; secret values are represented only by named domain-separated capability commitments.

Before execution, retained verified buffers for the wrapper, runtime, script, dependency lock/tree/manifest, every dependency member, and every readable artifact are written into an owner-only `0700` temporary root with one-link `0400` or `0500` files under a `rootfs` layout that preserves module resolution paths. The child cwd, runtime, script, wrapper mount sources, artifact argv, and artifact environment values are rewritten to those copies. No original readable source path is passed for child consumption. The local synthetic wrapper is itself interpreted by the copied runtime; production uses the copied attested wrapper. Postflight still rejects source mutation, tracked mutation, or undeclared writes. macOS/local synthetic execution and production-shaped D1 subprocess tests report `production_ready=false` and `real_linux_wrapper_test_satisfied=false`; they cannot activate authority and do not claim a real Linux/Kami image, firewall, mount policy, or immutable image exists.

No production D1 database was created or mutated by this candidate. Before authority activation, an approved coordinator later applies the migration using an environment-held binding and obtains independently signed D1 and sandbox attestations conforming to `d1-live-attestation.schema.v2.json` and `linux-sandbox-attestation.schema.v2.json`. An admission reviewer independent from each subject then signs that subject's receipt, and the three receipts are committed. A later reviewed commit adds the exact three-entry registry, whose entries point back to the applicable strict-ancestor receipt commits; the live verifier runs only from a descendant of that registry commit. This ordering avoids any commit-hash cycle. The verifier safely opens the two attestations, exact tracked registry, and Ed25519 signing key once with `O_NOFOLLOW`; requires owner control, one link, restricted modes, canonical attestation JSON, full trust/admission validation, and pairwise identity independence before any query; retains descriptors and raw buffers across the live D1 await; and rechecks path/inode/link/metadata and descriptor bytes before signing and before output. Receipt hashes use only retained buffers. Any race removes or leaves absent the receipt. The verifier queries the selected D1 through the official query contract, compares exact canonical tables/indexes/triggers, verifies both external signatures and freshness, and writes a signed receipt. It emits no raw account/database IDs or tokens.

```bash
npx wrangler d1 migrations apply "$GATE_H2_LEDGER_DATABASE_BINDING" --remote --config "$GATE_H2_WRANGLER_CONFIG"
npm run dataset-factory:reviewed-metrics-pre-activation-verify-v2 -- --d1-attestation "$GATE_H2_D1_ATTESTATION_FILE" --sandbox-attestation "$GATE_H2_SANDBOX_ATTESTATION_FILE" --max-age-seconds 3600 --signing-key "$GATE_H2_PREACTIVATION_SIGNING_KEY_FILE" --verifier-principal "$GATE_H2_PREACTIVATION_PRINCIPAL" --verifier-session "$GATE_H2_PREACTIVATION_SESSION" --verifier-route "$GATE_H2_PREACTIVATION_ROUTE" --output "$GATE_H2_PREACTIVATION_RECEIPT_FILE"
```

The concrete Kami wrapper/image build, real resolver-produced complete runtime/module/native member inventory, and independent sandbox-attestation producer are still prerequisites and are not satisfied here. Each real admission receipt and the exact three-entry registry must be reviewed and committed before running the verifier and before the authority-only direct child. The real later Linux test must execute every adversarial `stage-run` case in the immutable image and confirm observed firewall, mount, destination-capability, write-confinement, copied-path, and dependency-resolution evidence before the three artifacts may be pinned. The tracked tests use internal adapters, production-shaped Cloudflare HTTP mocks, disposable local SQLite, synthetic closure proofs, and a local pass-through wrapper; they prove local orchestration, exact-byte retention, deterministic typed grammar, private-copy rewriting, source-race rejection, publication race rejection, and failure ordering, but not future provider permissions, actual Linux resolver completeness, or Linux enforcement.

Security subprocesses never resolve `git`, `openssl`, `curl`, `ioreg`, `sw_vers`, or `uname` through `PATH`. OS-specific absolute paths are realpath checked, opened without following symlinks, and fstat verified as root-owned regular files with no group/world write before argument-array execution without a shell. PATH shadows, writable replacements, and aliases are adversarially tested. AWS tests also require exact unsupported-region and pinned-fingerprint mismatch codes.

AWS verification follows the primary [EC2 identity verification procedure](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/verify-iid.html) and [region certificate registry](https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/regions-certs.html). Cloudflare privacy proof follows the primary [R2 public bucket model](https://developers.cloudflare.com/r2/buckets/public-buckets/) and read-only custom/managed domain endpoints in the [R2 bucket API](https://developers.cloudflare.com/api/resources/r2/subresources/buckets/subresources/domains/).

The endpoint decoders follow Cloudflare's primary [Workers scripts list](https://developers.cloudflare.com/api/resources/workers/subresources/scripts/methods/list/), [script settings](https://developers.cloudflare.com/api/resources/workers/subresources/scripts/subresources/script_and_version_settings/methods/get/), [per-script subdomain](https://developers.cloudflare.com/api/resources/workers/subresources/scripts/subresources/subdomain/methods/get/), [dispatch namespaces](https://developers.cloudflare.com/api/resources/workers_for_platforms/subresources/dispatch/subresources/namespaces/), [Pages projects](https://developers.cloudflare.com/api/resources/pages/), [Workers custom domains](https://developers.cloudflare.com/api/resources/workers/subresources/domains/), [zone routes](https://developers.cloudflare.com/api/resources/workers/subresources/routes/), and [D1 query](https://developers.cloudflare.com/api/resources/d1/subresources/database/methods/query/) references.

High-impact adversarial tests require stable `H2_*` boundaries and named intended/observed codes. The tracked negative-code registry declares the exact expected code for every baseline and source-search rejection; message-derived codes normalize temporary roots, and self-test reports zero generic expectations and zero mismatches. Coverage includes inventory omissions/extras, same-host aliases and conflicting claims, wrong identity signatures, stale/not-yet-valid/event-outside receipts, wrong nonce/candidate/authority/role, local and remote symlink and `/tmp` aliases, exact-byte races, and wrong provider/null ETag/key/finalization/chronology/synthetic-without-capability retention. The 63-row hand oracle and all prior entity, place, chronology, source, matrix, publication, and privacy controls remain active. All temporary roots clean in `finally`.

The tracked supersession candidate notice pins v1 exactly and says v2 does not yet exist as a publication. It preserves the 32 v1 accepted task decisions while proposing to supersede only v1's historical close interpretation, unavailable-denominator satisfaction, blank-template evidence references, and task-acceptance-only completion logic.
