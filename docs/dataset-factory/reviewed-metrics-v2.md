# Reviewed Metrics v2 Candidate

Reviewed Metrics v2 is a candidate-only blind evaluation contract for issue #96. It freezes 36 image-mode tasks, two OCR crops, six neutral entity scenes, 16 aerial land-use tasks, and 18 abstention decisions before prediction. These 78 task memberships resolve to 44 unique source pixels. A separate source-only contract uses an opaque official-source-search task ID. The candidate and both issue completion booleans are false. No real authority, prediction, freeze, private expected envelope, gold, score, task review, publication, or production mutation exists.

The coordinator input authority contains the exact source hashes, normalized pixel hashes, dimensions, component/split bindings, nullable per-input rights values, predecessors, fixed membership, and source-to-opaque mapping. It must never be copied into a predictor route. The normal bundle command reads only the committed `input-authority-v2.json` at its fixed path and requires exactly 56,908 bytes with SHA-256 `34be01a2750894eab27ad8882acb79a0366f1de9b6c85fb96bb84e95ecfd81fa`; there is no `--authority` override. It also regenerates and compares the complete semantic authority, including the exact `v2-0001` through `v2-0044` mapping. This is non-circular because the authority records `candidate_commit=null`; a later execution authorization separately binds the reviewed candidate commit.

Rights values are copied exactly: `null` is not coerced to `false`, and completeness is not invented. The three Phase D controls are read from the registered ignored Gold evidence route and verified against the planned exact bytes; no source payload is duplicated in Git and no private object locator is recorded in the candidate. All six ground-scene mappings join by neutral ID through exact `ground-originals-v1/records-v1.json` bytes. Both OCR crops additionally join their parent, record, region coordinates, and crop hash through exact `reviewed-visual-transcriptions-v1.json` bytes. Both authorities are pinned globally and per applicable input.

The candidate records implementation base `5fe4dfbe51a320a51f1f126b4a2d8cf0722be5dc` separately from `candidate_commit=null`. This avoids a self-referential fixture hash. A later reviewed authority must bind the actual candidate commit before any execution path can exist.

## Blind bundle

The caller supplies a new absolute temporary route whose parent already exists. Every existing ancestor is checked with `lstat` and `realpath`; symlink parents/leaves, traversal, existing outputs, repository/private/PKM overlap, extra files, duplicate opaque IDs/hashes, source drift, normalized-pixel drift, metadata, and authority substitution are refused. The output directory is created exclusively with an invocation-secret owner marker and inode/device binding. A failure recursively removes only that newly created child after marker and inode verification; an arbitrary caller-supplied route is never recursively removed.

Each source is decoded and written as deterministic lossless PNG with an opaque filename. Every ancillary PNG chunk, including `pHYs`, is stripped; verification requires only critical chunks and zero EXIF, IPTC, XMP, comments, ICC profiles, or thumbnails. The predictor-visible route contains exactly 44 uniformly shaped neutral media rows, neutral instructions requiring the same full-superset output for every row, the exact tracked `prediction-output.schema.v2.json` bytes, and a descriptor that binds those schema bytes. It exposes no purpose, subset, membership, source mapping, per-task count, or self-authored mount attestation. Semantic no-inference tests reject task-family words in descriptor/instructions.

```bash
npm run dataset-factory:reviewed-metrics-verify-v2
npm run dataset-factory:reviewed-metrics-build-blind-bundle-v2 -- --output /tmp/new-opaque-route
npm run dataset-factory:reviewed-metrics-build-source-search-bundle-v2 -- --output /tmp/new-public-source-route
npm run dataset-factory:reviewed-metrics-self-test-v2
npm run dataset-factory:reviewed-metrics-integration-test-v2
npm run dataset-factory:reviewed-metrics-verify-tracked-v2
npm run dataset-factory:artifacts:check -- --verify-files --verify-required-only --require dfv0_reviewed_metrics_v1_publication,dfv0_reviewed_metrics_v2_candidate_20260715
npm run typecheck --workspace=@mtl-archives/scripts
```

Prediction, gold, result, source-search, criterion-matrix, publication, and task-review contracts fail closed on completed states. Visual gold retains the exact 36/2/6/16/18 universes, but it contains no source-task decision or private expected commitment. Before prediction, the source-only route receives a public organization-name query subject, `The Gazette`, plus a neutral prompt, an opaque task ID, a generic HTTPS Quebec-government hostname-suffix policy, a generic deterministic normalization contract, and exact bundle-local output-schema bytes. The subject is a public task input selected from the accepted official-source chain so a bundle-only researcher can identify the record; it is neither a scored answer field nor gold. The bundle contains no year, civic number, street, city/place, canonical target URL, repository path, Gate E identifier, dossier, commitment, salt, predecessor pin, source-specific hostname/path, or post-review artifact. The two-file bundle bytes, inventory, schema, and deterministic joins are code/tool evidence. Repository and history denial is instead an operational requirement attested and evidenced on the isolated Kami/Crabbox surface by a reviewed sanitized sync manifest, fresh route and remote inventory, then cleanup/readback; the validator does not prove the remote filesystem state. Its output contains only public year/address/place/official-URL fields and a bounded paraphrase; it does not invent or echo internal source or representation IDs.

Both the visual and source-search predictions are frozen before any completed visual gold review or source dossier/private-envelope work. The dossier follows source freeze; envelope sealing follows the dossier; private scoring is strictly after the envelope seal. Authority identities and timestamps join that order through results, matrix, and publication validation. The private envelope has an exact 32-byte lowercase-hex salt and CSPRNG metadata explicitly recorded under an `attestation_id` as an author attestation rather than an independently verifiable receipt or proof. Salt generation must satisfy `source_search_freeze_at < generated_at <= authored_at == private_envelope_sealed_at`; weak, repeated, low-diversity, sequential, known deterministic, and non-CSPRNG declarations are rejected. The internal synthetic capability alone may use one derived high-diversity deterministic test salt. The commitment is `SHA256("gate-h-v2-search-expected\0" || salt || "\0" || JCS(expected_answer_v2))`.

The deterministic private scorer opens that commitment, validates the exact frozen prediction, and compares its public fields against the post-freeze private envelope under the bundle-visible normalization contract: NFKC and whitespace normalization, case/diacritic/punctuation-insensitive text comparison, common French/English street-designator normalization, digit normalization, and canonical official HTTPS URL comparison. Provenance is not accepted from tracked constants: it dynamically joins the independently authored dossier/envelope to the sole accepted Gate E promotion and independent review receipt, representation, and acquisition. Gate E history remains tracked and readable. The salt and commitment protect envelope integrity and preserve blinding across the ordered workflow; they do not claim secrecy from a reader who independently inspects that history.

Protected finalization opens each authority/task/bundle/prediction/freeze/envelope path descriptor-first with `O_NOFOLLOW`; `fstat` must show an owner-controlled regular one-link file at exact mode `0400` or `0600`, and the path must still name that descriptor. Parsing, hashing, scoring, and final checks use the same retained bytes, including exact rereads that reject same-inode, same-size, restored-mtime rewrites. Canonical private detail is created `O_CREAT|O_EXCL` at `0400`, then the file and directory are fsynced and exact-read back. The evaluator key follows the same descriptor-first/path-inode discipline, requires one link and exact mode `0600`, must be Ed25519 and derive the authority public key, and must remain inside the evaluator root but outside repository and PKM trees.

No production command writes R2. `score-source-search` first prepares the sealed detail when no retention receipt exists and returns `private_detail_prepared_retention_required`. A later no-network invocation ingests `--retention-receipt`; that evaluator-signed receipt must prove byte-identical content-addressed PRIVATE storage/readback for the exact envelope and detail hashes under opaque object-key hashes, bind a provider/bucket capability ID plus version/etag where available, attest no public ACL, and order write/readback/issue times. The final public receipt embeds and signs that verified retention evidence. Synthetic tests use only an internal in-memory private store. Envelope/detail remain excluded from publication.

OCR gold normalization is not an authored target: `ocr_normalized` must exactly equal the documented deterministic normalization of `ocr_raw`, and the scorer derives its gold sequence from `ocr_raw`. Exact match compares those derived sequences; CER and WER use Levenshtein edits over Unicode code points and whitespace-delimited normalized words. A zero gold sequence has an honest zero denominator and undefined rate rather than an invented denominator, although completed v2 gold rejects empty OCR support. Entity mentions require nonblank normalized surfaces, positive-area `[x1,y1,x2,y2]` boxes within `[0,1]`, controlled types, and at most 12 mentions per row. Duplicate rejection quantizes each coordinate to 1/10,000 and also rejects same normalized surface/type pairs with IoU at least `0.98`, closing epsilon-shift inflation while allowing clearly disjoint repeated visible text. Detection precision/recall uses bounded deterministic polynomial min-cost assignment, maximizing valid match cardinality first, integerized total IoU second, and a stable pair tie-break; compatibility still requires exact normalized surface/type and IoU at least `0.5`. Every linked identity is evaluated only against its matched gold mention, and an unmatched linked assertion is unsafe.

The place metric universe is exactly `[task-7b1d4f0c9a2e6d83]`, the opaque SOURCE-ONLY official-search task. Precision is `correct supported source tasks / predicted source tasks`; coverage is `predicted source tasks / reviewed supported source tasks`. The task is joined through the accepted Gate E claim, source representation/acquisition, frozen source prediction, private envelope/detail retention, and signed finalization. Visual prediction `place_links` and visual gold `place_opportunities` are schema- and runtime-forbidden, so arbitrary scene or mention attachment cannot create support. `no_pixel_identity_claim` remains true: this metric says nothing about scene geolocation. Scene-level place opportunity support is zero/unavailable unless a future independent scene-level authority is introduced.

## Complete metric universe

Completed results pin the committed authority copy, raw visual prediction/freeze, gold, exact source task, and signed public private-score receipt. Validation deterministically recomputes exactly 63 rows: OCR normalized exact match/CER/WER; entity precision/recall/false-identity rate; reviewed-source-only place precision/coverage; per-class precision/recall/F1 for all five image classes plus macro-F1 and raw confusion/support; aerial exact-set accuracy/Jaccard, eight-label precision/recall/F1, and micro precision/recall/F1; abstention coverage/rate, appropriate-abstention recall, unsafe non-abstention rate, selective error, and decision accuracy; mask-IoU and geolocation-distance prerequisite N/A rows; actual visual/source operation timing; and explicitly unavailable model/tool cost. Reviewed Metrics v2 has no usage-receipt ingestion path and accepts only that explicit unavailable cost state.

Every row carries the exact fixed and included opaque IDs, exclusions fixed before scoring, raw counts, numerator, denominator, value or honest undefined/N/A, limitations, support, and provenance. Failed predictions and abstentions remain in their applicable fixed denominators. Aerial rows retain the exact 16-ID universe while explicitly excluding only IDs the independent gold review marked unreviewable before scoring; the included/excluded partition is complete and at least 12 IDs must remain reviewable. Mask and geolocation rows include no IDs and explicitly exclude their full fixed universes because the required reviewed masks or verified coordinates do not exist. Completion is derived from the exact metric universe plus all five positive image-class gold supports, positive entity gold support, at least 12 reviewable aerial inputs, and stop conditions including zero entity prediction support, leakage, route overlap, or chronology failure. Honest zero-support states remain visible; mask/geolocation N/A and cost-unavailable-without-receipt are the only expected non-observed final statuses. The normal `score` CLI becomes available only after both frozen prediction chains, completed gold, a signed passing private receipt, and byte-identical authority workspace copy validate. Reusable results validation requires `privateReceipt.scored_at < results.scoring.scored_at`, and matrix/publication inherit that check.

Final matrix rows use exact criterion IDs, result IDs, evidence roles, and filenames. They parse and semantically join both prediction/freeze chains, task, public receipt, results, task review, gold, and canonical tracked input authority. Published descriptors require exactly 15 public members, including the portable source-search output schema but never the private envelope or detail, their inherited and dynamic predecessor roles, no extra/missing/duplicate files, full semantic revalidation, authority-named publisher provenance, completion derived from the final matrix, and the descriptor as the commit-last marker.

The freeze implementation remains unavailable because the tracked execution authority does not exist. A later authority-only commit retains the exact direct-child, sole changed path, clean repository/index/worktree, and byte-identical HEAD/index/worktree controls. Source-search activation requires v2.2: one coordinator Ed25519 trust public key plus exactly seven canonical trusted inventory entries. Local entries carry a coordinator-verified stable host UUID. AWS/Kami entries carry either a verified signed EC2 instance identity-document digest and instance ID or a coordinator-verified SSH host fingerprint plus instance identity; the `aws_signature_verified` flag records which claim applies.

Every `surface_id` derives from exact canonical inventory bytes, and one inventory digest maps to exactly one ID. The coordinator trust key signs each one-shot receipt over candidate commit, authority binding hash, nonce, role, exact surface ID/inventory digest, requested route, canonical physical root on that surface, nearest-existing-ancestor device/inode evidence, issue time, and bounded expiry. Validation rejects self-signed host keys, duplicate inventories, relabeling, nonce replay, stale or wrong candidate/authority/role bindings, and same-surface equality/ancestry after canonicalization, including `/tmp`/`/private/tmp` and symlink aliases. Local evidence is physically re-resolved; remote canonical roots remain coordinator-attested operational evidence. This is not hardware remote attestation unless the AWS identity signature is actually verified.

High-impact adversarial tests require stable `H2_*` boundaries and named intended/observed codes. Coverage includes coordinator trust/signature and inventory relabel attacks; stale/wrong nonce/candidate/authority/role receipts; local and remote alias overlap; same-inode same-size restored-metadata rewrites for every finalization input and detail; symlink/hard-link inputs and keys; missing, wrong-signature, wrong-hash, failed-readback, or public-ACL retention; arbitrary visual scene and pixel-identity place attachment; epsilon duplicates; ambiguous ties; and a fully connected 12x12 assignment bounded under one second. The 63-row hand oracle independently constructs and compares every complete row object, including universes, partitions, raw counts, numerator/denominator, value/status, support, limitations, and provenance. All temporary roots clean in `finally`.

The tracked supersession candidate notice pins v1 exactly and says v2 does not yet exist as a publication. It preserves the 32 v1 accepted task decisions while proposing to supersede only v1's historical close interpretation, unavailable-denominator satisfaction, blank-template evidence references, and task-acceptance-only completion logic.
