# Reviewed Metrics v2 Candidate

Reviewed Metrics v2 is a candidate-only blind evaluation contract for issue #96. It freezes 36 image-mode tasks, two OCR crops, six neutral entity scenes, 16 aerial land-use tasks, and 18 abstention decisions before prediction. These 78 task memberships resolve to 44 unique source pixels. A separate source-only contract uses an opaque official-source-search task ID. The candidate and both issue completion booleans are false. No real authority, prediction, freeze, private expected envelope, gold, score, task review, publication, or production mutation exists.

The coordinator input authority contains the exact source hashes, normalized pixel hashes, dimensions, component/split bindings, nullable per-input rights values, predecessors, fixed membership, and source-to-opaque mapping. It must never be copied into a predictor route. The normal bundle command reads only the committed `input-authority-v2.json` at its fixed path and requires exactly 56,908 bytes with SHA-256 `34be01a2750894eab27ad8882acb79a0366f1de9b6c85fb96bb84e95ecfd81fa`; there is no `--authority` override. It also regenerates and compares the complete semantic authority, including the exact `v2-0001` through `v2-0044` mapping. This is non-circular because the authority records `candidate_commit=null`; a later execution authorization separately binds the reviewed candidate commit.

Rights values are copied exactly: `null` is not coerced to `false`, and completeness is not invented. The three Phase D controls are read from the registered ignored Gold evidence route and verified against the planned exact bytes; no source payload is duplicated in Git and no private object locator is recorded in the candidate. All six ground-scene mappings join by neutral ID through exact `ground-originals-v1/records-v1.json` bytes. Both OCR crops additionally join their parent, record, region coordinates, and crop hash through exact `reviewed-visual-transcriptions-v1.json` bytes. Both authorities are pinned globally and per applicable input.

The candidate records implementation base `5fe4dfbe51a320a51f1f126b4a2d8cf0722be5dc` separately from `candidate_commit=null`. This avoids a self-referential fixture hash. A later reviewed authority must bind the actual candidate commit before any execution path can exist.

## Blind bundle

The caller supplies a new absolute temporary route whose parent already exists. Every existing ancestor is checked with `lstat` and `realpath`; symlink parents/leaves, traversal, existing outputs, repository/private/PKM overlap, extra files, duplicate opaque IDs/hashes, source drift, normalized-pixel drift, metadata, and authority substitution are refused. The output directory is created exclusively with an invocation-secret owner marker and inode/device binding. A failure recursively removes only that newly created child after marker and inode verification; an arbitrary caller-supplied route is never recursively removed.

Each source is decoded and written as deterministic lossless PNG with an opaque filename. Every ancillary PNG chunk, including `pHYs`, is stripped; verification requires only critical chunks and zero EXIF, IPTC, XMP, comments, ICC profiles, or thumbnails. The predictor-visible route contains exactly 44 uniformly shaped neutral media rows, neutral instructions requiring the same full-superset output for every row, the exact tracked `prediction-output.schema.v2.json` bytes, and a descriptor that binds those schema bytes. It exposes no purpose, subset, membership, source mapping, per-task count, or self-authored mount attestation. Semantic no-inference tests reject task-family words in descriptor/instructions.

```bash
npm run dataset-factory:reviewed-metrics-verify-v2
npm run dataset-factory:reviewed-metrics-build-blind-bundle-v2 -- --output /tmp/new-opaque-route
npm run dataset-factory:reviewed-metrics-build-source-search-bundle-v2 -- --output /tmp/new-public-source-route
npm run dataset-factory:reviewed-metrics-self-test-v2
npm run dataset-factory:reviewed-metrics-integration-test-v2
npm run dataset-factory:reviewed-metrics-verify-tracked-v2
npm run dataset-factory:artifacts:check -- --verify-files --verify-required-only --require dfv0_reviewed_metrics_v1_publication,dfv0_reviewed_metrics_v2_candidate_20260715
npm run typecheck --workspace=@mtl-archives/scripts
```

Prediction, gold, result, source-search, criterion-matrix, publication, and task-review contracts fail closed on completed states. Visual gold retains the exact 36/2/6/16/18 universes, but it contains no source-task decision or private expected commitment. Before prediction, the source-only route receives a public organization-name query subject, `The Gazette`, plus a neutral prompt, an opaque task ID, a generic HTTPS Quebec-government hostname-suffix policy, a generic deterministic normalization contract, and exact bundle-local output-schema bytes. The subject is a public task input selected from the accepted official-source chain so a bundle-only researcher can identify the record; it is neither a scored answer field nor gold. The bundle contains no year, civic number, street, city/place, canonical target URL, repository path, Gate E identifier, dossier, commitment, salt, predecessor pin, source-specific hostname/path, or post-review artifact. The two-file bundle bytes, inventory, schema, and deterministic joins are code/tool evidence. Repository and history denial is instead an operational requirement attested and evidenced on the isolated Kami/Crabbox surface by a reviewed sanitized sync manifest, fresh route and remote inventory, then cleanup/readback; the validator does not prove the remote filesystem state. Its output contains only public year/address/place/official-URL fields and a bounded paraphrase; it does not invent or echo internal source or representation IDs.

Both the visual and source-search predictions are frozen before any completed visual gold review or source dossier/private-envelope work. The dossier follows source freeze; envelope sealing follows the dossier; private scoring is strictly after the envelope seal. Authority identities and timestamps join that order through results, matrix, and publication validation. The private envelope has an exact 32-byte lowercase-hex salt and CSPRNG metadata explicitly recorded under an `attestation_id` as an author attestation rather than an independently verifiable receipt or proof. Salt generation must satisfy `source_search_freeze_at < generated_at <= authored_at == private_envelope_sealed_at`; weak, repeated, low-diversity, sequential, known deterministic, and non-CSPRNG declarations are rejected. The internal synthetic capability alone may use one derived high-diversity deterministic test salt. The commitment is `SHA256("gate-h-v2-search-expected\0" || salt || "\0" || JCS(expected_answer_v2))`.

The deterministic private scorer opens that commitment, validates the exact frozen prediction, and compares its public fields against the post-freeze private envelope under the bundle-visible normalization contract: NFKC and whitespace normalization, case/diacritic/punctuation-insensitive text comparison, common French/English street-designator normalization, digit normalization, and canonical official HTTPS URL comparison. Provenance is not accepted from tracked constants: it dynamically joins the independently authored dossier/envelope to the sole accepted Gate E promotion and independent review receipt, representation, and acquisition. Gate E history remains tracked and readable. The salt and commitment protect envelope integrity and preserve blinding across the ordered workflow; they do not claim secrecy from a reader who independently inspects that history. The private detail may contain the opening and field results but is never a publication member. The public receipt exposes only artifact hashes/bytes, pass/failure codes, commitment, source-search freeze, and evaluator authority pins. Place precision/recall are derived only from a passing receipt for the opaque accepted source task; visual place rows and reviewer-authored booleans have no role. Task review pins the exact task, freeze, public receipt, private-detail hash/bytes, commitment, Gate E dossier pins, and distinct search-predictor/evaluator/task-review authorities. `private_score_passed` is intentionally absent from authored checks.

Completed results pin the committed authority copy, raw visual prediction/freeze, gold, exact source task, and public private-score receipt. Validation deterministically recomputes visual metrics from visual prediction/gold and both place rows exclusively from the passing source receipt. Required metrics with zero support can exist only as an incomplete stage result and cannot pass final task/matrix/publication gates. The normal `score` CLI becomes available only after both frozen prediction chains, completed gold, passing private receipt, and byte-identical authority workspace copy validate.

Final matrix rows use exact criterion IDs, result IDs, evidence roles, and filenames. They parse and semantically join both prediction/freeze chains, task, public receipt, results, task review, gold, and canonical tracked input authority. Published descriptors require exactly 15 public members, including the portable source-search output schema but never the private envelope or detail, their inherited and dynamic predecessor roles, no extra/missing/duplicate files, full semantic revalidation, authority-named publisher provenance, completion derived from the final matrix, and the descriptor as the commit-last marker.

The freeze implementation is authority-activatable without another code change, but it currently refuses because `docs/dataset-factory/authorities/reviewed-metrics-v2/execution-authorization-v2.json` does not exist. A later authority-only commit must have exactly one parent, declare that exact 40-hex parent as `candidate_commit`, change exactly the fixed tracked authority path and no other path, leave the repository clean, and expose identical bytes in `HEAD`, index, and worktree. Source-search activation requires the version-compatible v2.1 authority shape, which retains the v2.0 visual fields and adds source-search and post-freeze dossier/envelope timestamps. All seven principals, sessions, and normalized absolute filesystem routes are pairwise distinct, and no implementation, execution, or review route may equal, contain, or be contained by another. Deterministic injection exists only behind the unexported synthetic-test capability; the CLI has no authority override.

The tracked supersession candidate notice pins v1 exactly and says v2 does not yet exist as a publication. It preserves the 32 v1 accepted task decisions while proposing to supersede only v1's historical close interpretation, unavailable-denominator satisfaction, blank-template evidence references, and task-acceptance-only completion logic.
